[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["Max Prehoda <maxprehoda@gmail.com>"]
description = ""
name = "journal-app"
version = "0.1.0"
[tool.poetry.dependencies]
arrow = "^0.15.5"
python = "^3.7"
[tool.poetry.dev-dependencies]
